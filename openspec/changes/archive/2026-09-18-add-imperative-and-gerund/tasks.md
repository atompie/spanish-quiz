## 1. Types and tense metadata

- [x] 1.1 Add `'imperativo_afirmativo'` and `'imperativo_negativo'` to `TenseId` in `src/types/grammar.ts` and verify the project still typechecks (`npm run typecheck` or equivalent) with the two new ids unused so far
- [x] 1.2 Add optional `excludedPersons?: Person[]` to `TenseMeta` in `src/data/tenses.ts`, add entries for both new tenses with `excludedPersons: ['yo']` and appropriate `labels`/`labelEs`/`order`, and verify `TENSES` still has one entry per `TenseId` (no missing/duplicate ids)
- [x] 1.3 Add `gerund: string` to the `Verb` interface in `src/types/verb.ts`, next to `participle`, and verify the project fails to typecheck until every verb entry supplies it (confirms the field is wired through)

## 2. Conjugation generation helpers

- [x] 2.1 Add `conjugateImperativeAfirmativo(infinitive)` and `conjugateImperativeNegativo(infinitive)` to `src/data/verbTemplates.ts` for fully regular `-ar`/`-er`/`-ir` verbs, each returning a `Conjugations`-shaped object with a placeholder `yo: '—'`, and verify with a few known-regular verbs (e.g. "hablar" -> "habla", "no hables") by console-checking output against a reference conjugation
- [x] 2.2 Verify the two new template functions are exported and usable from `src/data/verbs.ts` the same way `conjugateRegular` is. **Implementation note**: `conjugateRegular` itself was extended to call both helpers internally and include `imperativo_afirmativo`/`imperativo_negativo` in its returned `Record<TenseId, Conjugations>`, so the ~76 verbs already calling `conjugateRegular(infinitive)` get correct Spanish imperative forms automatically with no per-verb code change — task 6.1 below only needs to add `gerund`, translations, and examples for those verbs, not conjugations. A `conjugateGerundRegular(infinitive)` helper was also added (not originally listed) to mechanically derive the gerund for regular verbs, analogous to the existing `conjugateRegular` pattern.

## 3. Quiz generator: honor `excludedPersons`

- [x] 3.1 Update `collectConjugationPool` in `src/lib/questionGenerator.ts` to skip a `(tense, person)` pair when `person` is in that tense's `excludedPersons`, and verify by running the conjugation quiz with `imperativo_afirmativo` enabled and confirming no "yo" question ever appears
- [x] 3.2 Verify existing tenses (presente, preterite, futuro, ir_a_infinitivo, acabar_de_infinitivo) are unaffected — all six persons still appear in their question pools (they have no `excludedPersons`, so the new filter is a no-op for them)

## 4. Verb list UI: gerund display

- [x] 4.1 Show `verb.gerund` in `src/components/common/ExplanationModal.tsx` next to the existing participle display, and verify visually (or via component test) that it renders for a sample verb. **Scope addition**: `specs/verb-conjugation/spec.md` ("Tenses declare which persons they cover") also requires the conjugation *grid* to honor `excludedPersons`, not just the quiz generator — this task list didn't call that out separately, so it was folded in here: the grid's person rows are now filtered by `tenseMeta.excludedPersons`, so "yo" never shows a row for imperativo tenses. Added `explanationGerund` i18n key (pl/en/de) alongside.
- [x] 4.2 Checked `src/components/verbs/VerbsListScreen.tsx`: no hardcoded tense list there. `SettingsScreen.tsx` and `storage.ts` (`DEFAULT_ENABLED_TENSES`) both derive from `TENSES`, so the two new tenses are automatically selectable/enabled with no code change needed.

## 5. Validation script

- [x] 5.1 Extend `scripts/validate-verbs.ts` to check every verb has non-empty `gerund`, and complete `imperativo_afirmativo`/`imperativo_negativo` conjugations + translations for every applicable (non-"yo") person, and verify it fails against a deliberately incomplete verb entry before the backfill, then passes after. Confirmed: running it now (before backfill) correctly reports missing `gerund` + missing imperative conjugations/translations for every verb.

## 6. Backfill regular verbs

- [x] 6.1 For each of the ~76 verbs currently using `conjugateRegular(...)` in `src/data/verbs.ts`, add `...conjugateImperativeAfirmativo(infinitive)` and `...conjugateImperativeNegativo(infinitive)` to their `conjugations`, add `gerund`, add pl/en/de translations for both imperative tenses, and add at least one phrase-quiz example per verb covering an imperative form; verify by running `validate-verbs.ts` after each batch. **Implementation note**: Spanish conjugations needed no per-verb edit (already automatic via `conjugateRegular`, see note on task 2.2). Applied via a one-off script (`/tmp/.../scratchpad/backfill-regular.ts`) that mechanically inserts hand-authored gerund/translations/examples into each verb's own block (scoped by id to avoid cross-verb corruption — an early version of the script had a bug where regex replacement matched the first unpatched block in the whole file rather than the target verb's own block; caught via validation before commit, fixed, and re-run from a clean backup). **Scope note on examples**: used 1 example per verb (imperativo_afirmativo, "tú" person) rather than one per each of the 5×2 person/mood combinations, consistent with this file's existing example density for other tenses (e.g. `hablar` has only 1 example for `ir_a_infinitivo`, not one per person) — interpreting spec's "at least one example sentence" as per-verb coverage of the imperative mood, not exhaustive per-person coverage.
- [x] 6.2 Run the full validation script and confirm zero missing-data errors for this group. Confirmed: after this batch, `validate-verbs.ts` reports errors only for the 61 still-pending irregular verbs — zero errors among the 76 regular verbs (or `quitar`/`andar`).

## 7. Backfill irregular verbs

- [x] 7.1 For each of the ~61 irregular verbs in `src/data/verbs.ts`, research and hand-write `imperativo_afirmativo` and `imperativo_negativo` conjugations (both are frequently irregular for irregular verbs, e.g. "ser" -> "sé"/"no seas"), add `gerund` (note irregular gerunds like "durmiendo", "yendo", "pidiendo"), add pl/en/de translations, and add at least one phrase-quiz example per verb; verify by running `validate-verbs.ts` after each batch. Applied via the same scoped-injection script pattern as task 6.1 (`/tmp/.../scratchpad/backfill-irregular.ts`), with all Spanish imperativo forms and gerunds hand-authored per verb (no template available for irregular verbs, per design.md Decision 3). Same example-density interpretation as 6.1 (1 example per verb). A few semantically awkward-but-grammatical commands were kept for completeness (e.g. modal verbs `deber`/`poder`/`querer`, and verbs like `nacer`/`morir`/`doler` where commanding the action is unusual but the form is valid Spanish/Polish/German).
- [x] 7.2 Spot-check a sample (~10 verbs) of the hand-written imperative/gerund forms against a reference conjugator to catch authoring mistakes. Spot-checked `ser`, `ir`, `estar`, `tener`, `hacer`, `decir`, `dormir`, `pedir`, `dar`, `oír` — all Spanish imperativo/gerund forms match standard references (irregular tú-affirmatives ser→sé, ir→ve, decir→di, hacer→haz, tener→ten, poner→pon, salir→sal, venir→ven; stem-changing gerunds dormir→durmiendo, pedir→pidiendo, decir→diciendo; y-insertion gerunds ir→yendo, oír→oyendo, leer→leyendo, construir→construyendo).
- [x] 7.3 Run the full validation script and confirm zero missing-data errors for this group. `validate-verbs.ts` reports "OK — 139 czasowników, wszystkie wpisy kompletne." (139 = 137 original + `quitar` + `andar`), zero errors across all verbs.

## 8. Add the two new verbs from the original request

- [x] 8.1 Add `quitar` (regular `-ar`, "zdejmować"/"to take off"/"ausziehen") to `src/data/verbs.ts` using `conjugateRegular` + the new imperative helpers, with pl/en/de translations for all tenses and a set of examples (clothing-removal themed, per earlier discussion), and verify it passes `validate-verbs.ts`. Passes clean.
- [x] 8.2 Add `andar` (regular except irregular preterite: anduve/anduviste/anduvo/anduvimos/anduvisteis/anduvieron; "chodzić"/"to walk"/"gehen") to `src/data/verbs.ts` with hand-written preterite and imperative forms (imperative for "andar" is regular: anda/ande/andemos/andad/anden and no andes/etc.), pl/en/de translations, and examples, and verify it passes `validate-verbs.ts`. Used `{ ...conjugateRegular('andar'), preterite: {...} }` spread-override — the first use of that pattern in the file (see design.md Decision 1 note); passes clean.

## 9. Final verification

- [x] 9.1 Run the full test suite / typecheck / lint and confirm all pass. `npx tsc -b --noEmit`: 0 errors. `npx vitest run`: 4 test files, 62 tests, all passed. `npm run lint` (oxlint): only pre-existing warnings in unrelated files (`LanguageContext.tsx`, `usePwaUpdate.ts`, `DialogPracticeScreen.tsx`, `useDialogSession.ts`), none touched by this change, no new warnings or errors.
- [x] 9.2 Manually run a conjugation quiz with imperative tenses enabled and a phrase quiz including imperative examples, confirming correct forms are accepted and "yo" never appears for imperative questions. Verified via a Node script exercising `generateQuiz` directly (equivalent to running the UI): generated 500 conjugation questions with all tenses enabled — zero questions had `tense` in `imperativo_afirmativo`/`imperativo_negativo` with `person: 'yo'`. **Bug caught by this check**: 16 examples (across both the regular and irregular backfill batches — `sufrir`, `perder`, `doler`, `enfermar`, `vomitar`, `lastimar`, `romper`, `llorar`, `enojar`, `preocupar`, `caer`, `morir`, `gastar`, `molestar`, `fumar`, `tocar`) had a negative-imperative Spanish sentence (e.g. "no llores más") mislabeled with `tense: 'imperativo_afirmativo'` — the per-verb data tables used a negative-flavored example sentence for some verbs but the injection script always stamped `tense: 'imperativo_afirmativo'`/id suffix `-imperativo_afirmativo-tu` regardless of content. Fixed by relabeling all 16 to `imperativo_negativo` (tense + id), re-verified zero mismatches remain, `validate-verbs.ts` still clean, and `tsc`/`vitest` still pass.
